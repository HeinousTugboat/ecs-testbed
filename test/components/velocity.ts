import 'mocha';
import { expect } from 'chai';

import { VelocityComponent } from '../../src/components/velocity';

describe('Components:', () => {
  describe('VelocityComponent', () => {
    it('exists', () => {
      expect(VelocityComponent).to.exist;
    });
  });
});
